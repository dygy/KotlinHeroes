package main

fun main () {
    val list = mutableListOf<Any>()
    val numberOfI = readLine()?.toInt()
    var i = 0
    var fails = 0;
    while (i < numberOfI!!*2) {
        val values = readLine()?.split("")!!
        list.add(values)
        i++
    }
    i = 0
    while (i < list.size) {
    if (list.get(i) as )
        if (i > 4) {

        }
        i++
        println(fails)
    }
}

